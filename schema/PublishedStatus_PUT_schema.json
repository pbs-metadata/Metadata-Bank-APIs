{
  "type": "object",
  "required": [
    "PublishedStatus"
  ],
  "properties": {
    "PublishedStatus": {
      "type": "string",
      "pattern": "^(.*)$",
      "enum": [
        "In Progress",
        "Externally Published",
        "Internally Published"
      ]
    }
  }
}