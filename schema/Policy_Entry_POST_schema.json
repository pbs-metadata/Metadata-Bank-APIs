{
    "title": "The Root Schema",
    "required": [
        "EntryClass",
        "EntryUID"
    ],
    "type": "object",
    "properties": {
        "EntryClass": {
            "minLength": 1,
            "pattern": "^(.*)$",
            "type": "string"
        },
        "EntryUID": {
            "minLength": 1,
            "pattern": "^(.*)$",
            "type": "string"
        }
    }
}